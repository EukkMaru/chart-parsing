# Claims

Create one file per bounded, falsifiable claim using
`../templates/CLAIM.md`. Name files with a stable topical identifier, for
example `tap-candidate-order.md`; do not name them only by a mutable function
address. Claims hold reasoning and anchors. Clean-room normative behavior belongs
in `spec/`, while session chronology belongs in `research/sessions/`.
