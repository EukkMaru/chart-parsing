#include <chart/reconstruction.hpp>

int main() {
    // The initial test only verifies that the clean-room target is consumable.
    // Replace this with focused behavioral tests as rules are reconstructed.
    return 0;
}
